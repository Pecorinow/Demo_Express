const { Request, Response } = require('express');

const proprietyValidatorMiddleware = () => {
    /**
     * @param {Request} req
     * @param {Response} res
     */
    const bodyToCheck = req.body;
    
}
    


module.exports = proprietyValidatorMiddleware;